sudo: required
services:
  - docker

before_install:
  - docker build -t boryslavkrymeniuk/docker-training -f Dockerfile.dev .

script:
  - docker run boryslavkrymeniuk/docker-training CI=true npm run test -- --coverage
